```
Open Source dependencies:
------------------------------
Flask : a lightweight Python web framework used to power the API layer.
python-dotenv : for managing environment variables securely through .env files.
LlamaIndex : the core RAG framework that powers data ingestion, context retrieval, and query orchestration.
Qdrant : an open-source vector database used for efficient similarity search and semantic retrieval.
Tree-sitter : an incremental parsing library for structured code analysis and language parsing.
HTTPX : an async-capable HTTP client for handling API communication.
OpenAI SDK : the client library to connect with Azure OpenAI services for embedding and model inference.
```